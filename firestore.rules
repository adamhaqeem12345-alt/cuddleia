
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow server-side processes (like your server actions) to write to the orders collection.
    // This rule denies all direct client-side access.
    match /orders/{orderId} {
      allow read, write: if false;
    }
  }
}
